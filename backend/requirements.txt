
# --- Web framework ---
fastapi>=0.111,<0.112
uvicorn[standard]>=0.29
python-multipart>=0.0.9        # form parsing, needed for a couple of OAuth callback shapes
email-validator>=2.1           # pulled in by pydantic's EmailStr (auth/team schemas)

# --- Config / validation ---
pydantic>=2.6
pydantic-settings>=2.2

# --- Database ---
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
alembic>=1.13                  # schema.sql bootstraps fresh DBs; alembic covers future migrations

# --- Auth ---
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7.4

# --- Background jobs / cache / realtime ---
celery>=5.3
redis>=5.0

# --- Agents / orchestration ---
langgraph>=0.1.5
autogen-agentchat>=0.2

# --- Memory (C1) ---
chromadb>=0.5
mem0ai>=0.1

# --- Safety agents (C2) ---
bandit>=1.7
pylint>=3.1
semgrep>=1.66

# --- HTTP client (Ollama, GitHub/Google OAuth, OPA) ---
httpx>=0.27

# --- Testing ---
pytest>=8.1
pytest-asyncio>=0.23