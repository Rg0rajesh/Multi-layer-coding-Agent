// js/config.js
// Edit these two values if you actually configure GitHub/Google OAuth apps.
// Left blank by default — same as frontend/.env.example in the original
// project, where VITE_GITHUB_CLIENT_ID / VITE_GOOGLE_CLIENT_ID are empty
// unless you've registered real OAuth apps.
window.AGENTX_CONFIG = {
  GITHUB_CLIENT_ID: "",
  GOOGLE_CLIENT_ID: "",
};
